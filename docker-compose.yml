services:
  db:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: mvrx
      POSTGRES_USER: mvrx
      POSTGRES_PASSWORD: mvrx
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
