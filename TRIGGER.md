## Quick Reference

```bash
# Run the dev server (tasks execute locally - make sure to point the TRIGGER_SECRET_KEY to the dev key)
npx trigger.dev@latest dev

# Deploy tasks to production
npx trigger.dev@latest deploy

# Deploy to a specific environment
npx trigger.dev@latest deploy --env staging
```
