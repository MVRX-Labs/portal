repos:
  - repo: local
    hooks:
      - id: typecheck
        name: TypeScript type-check
        entry: npx tsc --noEmit
        language: system
        types_or: [ts, tsx]
        pass_filenames: false
